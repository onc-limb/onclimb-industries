# onclimb-industries

Claude Code のスキル群を管理する個人用リポジトリです。
このリポジトリを作業ディレクトリとして Claude Code を起動し、日本語で指示を出すと、
対応するスキルが起動して成果物（記録・レポート・ドキュメント・図・台帳など）を生成します。

## 使い方

1. このリポジトリのルートで Claude Code を起動する。
2. 下の早見表のような指示を出す（スキル名を知らなくてよい。発話から該当スキルが起動する）。
3. スキルが必要に応じて対話でヒアリングし、成果物を出力する。
   データ系の出力はリポジトリ直下の git 管理外ディレクトリ（`worklog-data/` 等）、
   プロジェクト固有の成果物は `projects/<project>/` 配下に保存される。

調査・実装の対象となる実プロジェクトは `projects/` 配下に git clone して置く（後述）。

## 指示 → 成果物の早見表

代表例のみ。**全 43 スキルの詳細（起動フレーズ・入出力・保存先）は [`docs/skills-catalog.md`](docs/skills-catalog.md) を参照。**

| こう指示すると | こういう成果物ができる |
|----------------|------------------------|
| 「今日の作業をまとめて」 | 会話・操作ログを整理した digest（`worklog-data/`） |
| 「今日の作業を案件ごとに記録して」 | 案件×日付の作業記録 Markdown（`report-record/`） |
| 「今日の報告スライド作って」 | 依頼者向け日次報告 HTML スライド（`report-deck/`） |
| 「この transcript を議事録化して」 | 決定事項・TODO 付きの共有用議事録（`giziroku/`） |
| 「ナレッジベース作って」 | Obsidian 形式の技術ノート vault（`knowledge-base/`） |
| 「ブログ記事のドラフト書いて」 | 機密マスキング済みの技術記事ドラフト（`articles/`） |
| 「このプロジェクトの README / 設計書作って」 | コード調査に基づく設計書・README・ADR 草案 |
| 「この要件で提案書を作って」 | 工数内訳表付きの提案書・見積ドラフト |
| 「ToDo 追加」「棚卸しして」「優先順位つけて」 | タスク台帳の更新・優先度評価（`todo-data/todos.json`） |
| 「〜をやりたいので Issue 化して」 | 実装計画としての開発 Issue（GitHub 登録も可） |
| 「このプロジェクトをレビューして」 | 観点別・重要度ラベル付きのコードレビュー指摘 |
| 「オンボーディング資料を作って」 | 初見コードベースの地図・フロー・読む順番の資料 |
| 「この処理の流れをシーケンス図にして」 | 実コード追跡に基づく Mermaid シーケンス図 |
| 「API / DB スキーマ / インフラ構成を設計して」 | 壁打ちを経たスキーマ・ER 図・構成図 + 設計記録 |
| 「技術負債を洗い出して」「リリースできるか確認して」 | 根拠・優先度付きの監査レポート |
| 「Next.js と Remix を比較して技術選定して」 | 出典付き比較表 + 推奨（`tech-selection/`） |
| 「競合を調べて」「単価相場を調べて」 | 出典付きの市場・相場調査レポート |
| 「今月の請求書を作って」 | インボイス要件を満たす請求書 + 内訳表（`invoice-data/`） |
| 「今月の稼働時間を集計して」 | 案件別・日別の稼働サマリ（`timesheet-data/`） |
| 「レシート集計して」「今月の家計をまとめて」 | 家計・共同支出の月次集計（`budget-data/` 等） |
| 「この契約書をレビューして」 | 要注意条項と確認事項の整理レポート |
| 「思考を整理したい」 | 課題の本質を掘り下げた思考整理ドキュメント |
| 「学習ロードマップを作って」 | 週次計画付きの学習ロードマップ（`learning-roadmaps/`） |

## ディレクトリ構成

```
.
├── .claude/skills/   # スキル本体（<prefix>-<name> で命名。規約は .claude/skills/README.md）
├── personas/         # 分類（プレフィックス）ごとの共通ルール・文体・モデル選定ガイド
├── docs/             # スキル一覧・設計メモ
│   └── skills-catalog.md   # 全スキルの詳細一覧
├── ideas/            # スキルのアイデア・skill-feedback（改善インボックス）
├── mcp-servers/      # 自作 MCP サーバー（Google Tasks 同期など）
├── projects/         # 実プロジェクトの git リポジトリを置く作業場（git 管理外）
└── *-data/, giziroku/, knowledge-base/, report-*/ など
                      # 各スキルのデータ・成果物置き場（git 管理外）
```

### スキルの分類（プレフィックス）

| プレフィックス | 分類 |
|----------------|------|
| `jarvis` | 作業記録・一次資料系（読み手は AI か自分） |
| `friday` | 共有ドキュメント系（読み手は上司・クライアント・チーム） |
| `arc-reactor` | コーディング・レビュー系 |
| `edith` | 調査・データ収集・分析系 |
| `ultron` | 事務・金融・資産系 |
| `karen` | 一時利用・汎用系 |
| `vision` | プライベート・人間関係系 |

詳細は [`.claude/skills/README.md`](.claude/skills/README.md) と [`personas/`](personas/) を参照。

## projects ディレクトリ

実際のプロジェクトの git リポジトリを配置し、その中での作業やソースコード調査を行うための作業場です。

- `.gitignore` で **git 管理外**（当リポジトリでは追跡しない）。中身は各プロジェクト自身のリポジトリで管理する。
- プロジェクト固有の情報（ドキュメント・報告書・思考の整理など）は、そのプロジェクトのディレクトリ配下に
  プロジェクト固有として保存する。詳細な運用ルールは [`CLAUDE.md`](CLAUDE.md) を参照。

### iron-legion（自動実行ワークフロー群）

`projects/iron-legion/` は、自律的に働くワークフロー群を収容する private リポジトリで、
`projects/` 配下のプロジェクトの実装・改善に頻繁に使う。

| ワークフロー | 役割 | 使いどころ |
|--------------|------|------------|
| `extremis/` | 自己改善エンジニアリングループ（Elixir/OTP + Rust sentinel）。GitHub issue を渡すと分解 → 実装 → 検証 → レビュー → PR → マージまで自律実行 | issue ベースの実装を自律実行させたいとき（`extremis -p <project> epic <issue番号>`） |
| `veronica/` | マネジメント側ディスカバリーループ（Python）。観点カタログで「何をなぜやるべきか」を洗い出し、Go/No-Go を経て extremis 向け issue を起票 | プロダクトに必要なもの（規約・監視・CI/CD・コスト等）の洗い出し・issue 化をしたいとき |

- 対象プロジェクトは `projects/iron-legion/projects.toml` に登録し、`-p/--project` で選択する。
- `projects/iron-legion-self/` は extremis の自己改善専用クローン。通常の作業は
  `projects/iron-legion/` 側で行う。
- 詳細は `projects/iron-legion/README.md` と各ワークフローの README、および
  [`CLAUDE.md`](CLAUDE.md) の「iron-legion」節を参照。

## スキルを作る・育てる

- 新規スキルの作り方（分類の決定 → persona 参照 → 命名）は [`CLAUDE.md`](CLAUDE.md) を参照。
- 使っていて出た不満・改善点は [`ideas/skill-feedback.md`](ideas/skill-feedback.md) にため、
  たまったらまとめてスキルへ反映する運用にしている。
