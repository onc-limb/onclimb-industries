# CLI コマンド品質レポート — {{DATE}}

> 出典: `{{SOURCE_ROOT}}` / 走査ファイル {{FILES}} 件 / Bash 呼び出し {{INVOCATIONS}} 回
> 生成: edith-cli-command-quality（`scripts/analyze_commands.py` の集計を基に作成）

このレポートは Claude が実行した CLI コマンドのログを集計し、**人間もよく使うコマンドの
品質観点**と**危険コマンドの出現**を、人間側の学習に活かすためにまとめたもの。
数値はすべてスクリプトの決定論的集計であり、暗算ではない。

---

## 1. サマリ

- 対象: {{SOURCE_ROOT}}（{{FILES}} ファイル / {{INVOCATIONS}} Bash 呼び出し）
- ユニークコマンド数: {{UNIQUE}}
- 危険コマンド出現: {{DANGER_TOTAL}} 件（{{DANGER_KINDS}} 種類）
- 網羅できなかった範囲: Web 版(claude.ai)のログ、`$(...)` 内の入れ子コマンドの一部

## 2. 使用ランキング（人間もよく使うコマンド）

| # | コマンド | 回数 | 分類 | 品質フラグ |
|---|---------|-----:|------|-----------|
{{RANKING_ROWS}}
<!-- 例: | 1 | git | 312 | vcs | ⚠️ --force push ×2 | -->

## 3. 品質チェック観点（学習ポイント）

コマンドごとに「使うとき何を確認すべきか」。ログ中で実際に見つかったアンチパターンは
「実例」として添える（自分の癖の点検に使う）。

### {{COMMAND}}（{{COUNT}} 回, {{CATEGORY}}）

**確認すべき観点**
{{CHECKPOINTS}}

**ログ中で見つかったアンチパターン**
{{ANTIPATTERNS}}
<!-- なければ「なし（この期間では良好）」と書く -->

<!-- human_common かつ上位のコマンドを数件、この形式で繰り返す -->

## 4. 危険コマンド出現チェック

| 重大度 | パターン | 回数 | 対処 |
|--------|---------|-----:|------|
{{DANGER_ROWS}}

該当があった場合は、代表的な実行例と「なぜ危険か／どう直すか」を 1〜2 行で補足する。
重大度 critical / high は本人が必ず目視確認する。

## 5. ダッシュボード連携

- メトリクス JSON: `{{JSON_PATH}}`（スキーマ: `references/json_schema.md`）
- ダッシュボードプロダクトはこの JSON を読み込んでランキング・品質フラグ・危険コマンドを可視化する。

---

_出力は CLI 利用ログの機械的な集計・整理であって、実行の是非の最終判断ではない。
危険コマンドの該当は「確認を促すシグナル」であり、正当な用途もあり得る。_
