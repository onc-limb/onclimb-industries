<!--
README 雛形 (friday-design-doc-generator)
- 見出しは削除・順序変更しない。埋められないセクションは TBD と書く。
- 技術的記述には根拠パスを添える。推測は文末に「(推測)」タグ。
- {{...}} は生成時に置き換えるプレースホルダ。
-->

# {{project_name}}

<!-- 草案メタ情報: 共有前に残すか削るかをユーザーと判断する -->
> 生成日: {{generated_date}} / 対象: {{target_path}} / commit: {{commit_hash_or_TBD}}

{{1〜3 行の概要。このプロジェクトが何であるか。コードから確認できた範囲で書く}}

## 主な機能

<!-- コードから確認できた機能のみ。各項目に根拠パスを添える -->
- {{機能}}（`{{source_path}}`）
- TBD

## 技術スタック

<!-- manifest (package.json / pyproject.toml 等) と設定ファイルを根拠にする -->
| 区分 | 技術 | 根拠 |
|---|---|---|
| 言語 | {{language}} | `{{source_path}}` |
| フレームワーク | {{framework}} | `{{source_path}}` |
| TBD | TBD | — |

## ディレクトリ構成

```
{{主要ディレクトリのツリーと 1 行説明}}
```

## セットアップ

<!-- README・スクリプト・CI 設定から確認できた手順のみ。未確認の手順を創作しない -->
```bash
{{確認できたセットアップコマンド。無ければこのブロックを消して TBD}}
```

## 使い方

TBD

## 設定

<!-- 環境変数・設定ファイルは「キー名と用途」のみ。値・実クレデンシャルは絶対に転記しない -->
| キー | 用途 | 根拠 |
|---|---|---|
| {{ENV_KEY}} | {{用途}} | `{{source_path}}` |

## 開発（テスト・lint）

<!-- テストランナー・linter の起動方法。scripts / Makefile / CI 設定を根拠にする -->
```bash
{{確認できたコマンド}}
```

## TBD 一覧

<!-- 本文中の TBD をここに集約し、ヒアリング質問としてチャットにも提示する -->
- [ ] {{TBD 項目と、確認したい質問}}

## 参照したソース

<!-- 調査で根拠にしたファイルパスの一覧 -->
- `{{source_path}}`
