<!--
開発環境構築ガイド 雛形 (friday-procedure-doc-generator)
- 見出しは削除・順序変更しない。埋められないセクションは TBD と書く。
- 手順に載せる操作はすべて根拠パス付きの実在コマンドか、ユーザーの明言に基づくもの。
- 各ステップは「操作 → 期待結果」を揃える（期待結果が不明なら TBD）。
- 環境変数・シークレットは「キー名・用途・取得先」のみ。値は絶対に書かない。
- {{...}} は生成時に置き換えるプレースホルダ。
-->

# 開発環境構築ガイド: {{project_name}}

<!-- 草案メタ情報: 共有前に残すか削るかをユーザーと判断する -->
> 生成日: {{generated_date}} / 対象: {{target_path}} / commit: {{commit_hash_or_TBD}}

{{1〜2 行。このガイドを最後まで実行すると何ができる状態になるか（例: ローカルでアプリが起動し、テストが通る状態）}}

## 前提ツール

<!-- バージョン指定はバージョンファイル・manifest を根拠にする。根拠が無い場合は TBD -->
| ツール | バージョン | 根拠 |
|---|---|---|
| {{tool}} | {{version}} | `{{source_path（例: .tool-versions, .nvmrc, go.mod）}}` |
| TBD | TBD | — |

## リポジトリの取得

```bash
git clone {{repo_url_or_TBD}}
cd {{repo_dir}}
```

## 依存のインストール

```bash
{{確認できたインストールコマンド（例: npm ci / uv sync / go mod download）}}
```

- 期待結果: {{成功したときに何が起きるか（例: node_modules/ が生成され、エラーなく終了する）}}
- 根拠: `{{source_path}}`

## 環境変数・シークレットの設定

<!-- キー名と用途と取得先のみ。値・実クレデンシャルは絶対に書かない -->

```bash
{{例: cp .env.example .env}}
```

| キー | 用途 | 値の取得先 |
|---|---|---|
| {{ENV_KEY}} | {{用途}}（根拠: `{{source_path}}`） | {{どこで手に入れるか（例: チームの 1Password、リーダーに依頼）→ 不明なら TBD}} |

## ローカルサービスの起動（DB 等）

<!-- DB・キャッシュ・キュー等の依存サービス。docker-compose 等を根拠にする。不要なら「不要」と明記 -->

```bash
{{例: docker compose up -d db}}
```

- 期待結果: {{例: `docker compose ps` で db が healthy になる}}
- 根拠: `{{source_path}}`

{{マイグレーション・シードデータ投入があればここにステップを足す}}

## 起動と動作確認

```bash
{{起動コマンド}}
```

- 期待結果: {{例: http://localhost:3000 で画面が表示される}}
- 根拠: `{{source_path}}`

```bash
{{テスト・lint の実行コマンド（環境構築の完了確認を兼ねる）}}
```

- 期待結果: {{例: 全テストが pass する}}
- 根拠: `{{source_path}}`

## よくあるエラー

<!-- 既知のハマりどころ。既存ドキュメント・env-doctor の対応記録・ユーザーの明言が情報源。無ければ空のまま TBD -->
| 症状 | 原因 | 対処 |
|---|---|---|
| {{症状}} | {{原因}} | {{対処}} |

## TBD 一覧

<!-- 本文中の TBD をここに集約し、ヒアリング質問としてチャットにも提示する -->
- [ ] {{TBD 項目と、確認したい質問}}

## 参照したソース

<!-- 調査で根拠にしたファイルパスの一覧 -->
- `{{source_path}}`
