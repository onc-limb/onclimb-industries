{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "worklog 用 Hooks のテンプレート。<SKILL> をスキルディレクトリの実パス（例: /Users/you/project/.claude/skills/worklog）に置換し、プロジェクトの .claude/settings.json か ~/.claude/settings.json にマージして使う。SessionEnd と PreCompact のたびに collect.sh が走り、生ログを worklog-data/ へ退避する。データ置き場を変えたい場合は command 先頭に WORKLOG_DATA=/path を付ける。",
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash <SKILL>/bin/collect.sh"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash <SKILL>/bin/collect.sh"
          }
        ]
      }
    ]
  }
}
