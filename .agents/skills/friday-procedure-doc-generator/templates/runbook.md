<!--
運用手順書 雛形 (friday-procedure-doc-generator)
- 見出しは削除・順序変更しない。埋められないセクションは TBD と書く。
- 手順に載せる操作はすべて根拠パス付きの実在コマンドか、ユーザーの明言に基づくもの。
- 各ステップは「操作 → 期待結果 → 確認方法 → 根拠」を揃える（期待結果が不明なら TBD）。
- 破壊的・不可逆操作には ⚠️ と直前の確認ステップを置く。
- {{...}} は生成時に置き換えるプレースホルダ。<PLACEHOLDER> は実行時に実行者が埋める値。
-->

# 手順書: {{作業名（例: 本番デプロイ / 本番ロールバック）}}

<!-- 草案メタ情報: 共有前に残すか削るかをユーザーと判断する -->
> 生成日: {{generated_date}} / 対象: {{target_repo_or_env}} / commit: {{commit_hash_or_TBD}}

## この手順書の目的と適用場面

<!-- いつ・どんな状況でこの手順を実行するか。適用してはいけない場面があればそれも書く -->
{{1〜3 行。例: 本番環境へアプリケーションをデプロイする手順。緊急のホットフィックスにも同じ手順を使う}}

## 前提条件

<!-- 実行者が着手前に満たしているべき状態。権限・アクセス・ツール・タイミング -->
- 実行者: {{誰が実行するか / 必要な権限（例: AWS の deploy ロール、リポジトリの write 権限）}}
- 必要なツール: {{ツールとバージョン}}（根拠: `{{source_path}}`）
- 実行タイミング・承認: {{メンテナンスウィンドウ・承認フロー。コードから読めない典型項目 → TBD}}
- `<PLACEHOLDER>` の値の取得方法: {{手順中のプレースホルダをどこで確認するか}}

## 実行前チェックリスト

<!-- 着手してよいかの判断材料。1 つでも満たせないなら着手しない -->
- [ ] {{例: CI の main ブランチが green である（確認先 URL）}}
- [ ] {{例: 直前リリースの tag を控えた（ロールバックで使う）}}
- [ ] TBD

## 手順

<!-- 1 ステップ = 1 操作。番号順に実行すれば完了する構成にする -->

### 1. {{ステップ名}}

```bash
{{コマンド}}
```

- 期待結果: {{成功したときに何が起きるか}}
- 確認方法: {{どこを見て成功と判断するか}}
- 根拠: `{{source_path}}`

### 2. ⚠️ {{破壊的操作を含むステップ名}}

<!-- 破壊的操作の例。直前に確認内容を明示する -->
**実行前に確認**: {{何を見てから実行するか。失敗時の影響}}

```bash
{{コマンド}}
```

- 期待結果: {{...}}
- 確認方法: {{...}}
- 失敗した場合: {{→ トラブルシューティングの該当項目 / → 中断・ロールバック手順}}
- 根拠: `{{source_path}}`

## 作業後の確認

<!-- 手順完了後、作業全体が成功したと判断する基準 -->
- [ ] {{例: ヘルスチェックエンドポイントが 200 を返す（確認コマンド付き）}}
- [ ] TBD

## 中断・ロールバック手順

<!-- 「途中でやめて戻す」方法。埋まらない場合は TBD にし、ヒアリングの最優先項目とする。
     ロールバックが独立した手順書になる規模なら、別ファイルに分けてここからリンクする -->
{{どの時点まで進んでいたら何をすれば元に戻るか。ステップ書式は「手順」と同じ}}

## トラブルシューティング

<!-- 既知の失敗パターンと対処。過去の事故・env-doctor の対応記録・ユーザーの明言が情報源 -->
| 症状 | 原因 | 対処 |
|---|---|---|
| {{症状}} | {{原因}} | {{対処}} |

## TBD 一覧

<!-- 本文中の TBD をここに集約し、ヒアリング質問としてチャットにも提示する -->
- [ ] {{TBD 項目と、確認したい質問}}

## 参照したソース

<!-- 調査で根拠にしたファイルパスの一覧 -->
- `{{source_path}}`
